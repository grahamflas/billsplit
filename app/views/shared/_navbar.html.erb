<nav class="flex items-center justify-between bg-gray-800 text-white">
  <div class="p-6">
    <%= link_to(
        "Bill÷Split",
        root_path,
        class: "text-indigo-300 text-3xl font-bold tracking-[0.1rem]",
      ) %>
  </div>

  <div class="flex items-center gap-4 px-4">
    <% if current_user %>
      <div class="text-indigo-300"><%= current_user.email %></div>

      <%= link_to(
        'Edit profile',
        edit_user_registration_path,
        class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-white/15",
      ) %>

      <%= link_to(
        'Log out',
        destroy_user_session_path,
        data: { turbo_method: :delete },
        class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-white/15",
      ) %>
    <% else %>
      <%= link_to(
        'Sign up',
        new_user_registration_path,
        class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-white/15",
      ) %>

      <%= link_to(
        'Log in',
        new_user_session_path,
        class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-white/15",
      ) %>
    <% end %>
  </div>
</nav>
