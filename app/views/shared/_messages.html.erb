<% toasts = [] %>

<% flash.each do |message_type, message| %>
  <% if message.is_a?(String) %>
    <% toasts << message %>
  <% else %>
    <% message.each { |m| toasts << m } %>
  <% end %>
<% end %>

<%= mount_react_app("Toaster", props: { toasts: }) %>
