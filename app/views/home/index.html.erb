
<div class="flex justify-center items-center pt-20">
  <%= mount_react_app("GroupsContainer", props: {
    groups: @groups
  }) %>

  <%= link_to '+ Add Group',
    new_group_path,
    class: "rounded-md px-3 py-2 bg-indigo-400 hover:bg-indigo-500 focus:bg-indigo-500 text-white text-2xl"
  %>
</div>

<div class="flex flex-wrap justify-center gap-6 pt-6 pb-20">
  <%# render @groups %>
</div>
